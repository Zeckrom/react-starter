/*
 *
 * {{ properCase name }} actions
 *
 */

import { generateEmptyAction } from 'utils/generic-saga';
import { {{ camelCase name }}ActionTypes } from './constants';

const {{ camelCase name }}Action = generateEmptyAction({{ camelCase name }}ActionTypes.request);

export { {{ camelCase name }}Action };
