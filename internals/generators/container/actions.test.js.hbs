import { {{ camelCase name }}Action } from '../actions';
import { {{ camelCase name }}ActionTypes } from '../constants';

describe('{{ properCase name }} actions', () => {
  describe('Default Action', () => {
    it('has a type of {{ camelCase name }}ActionTypes.success', () => {
      const expected = {
        type: {{ camelCase name }}ActionTypes.request,
      };
      expect({{ camelCase name }}Action()).toEqual(expected);
    });
  });
});
